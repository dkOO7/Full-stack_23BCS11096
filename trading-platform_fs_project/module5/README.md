# Module 5: Deployment

This module is designated for deployment-related configurations and scripts.

## Files

- `deployment/Dockerfile`: A Dockerfile to containerize the Spring Boot backend application.
- `deployment/docker-compose.yml`: A Docker Compose file to orchestrate the backend container and a PostgreSQL database container.
